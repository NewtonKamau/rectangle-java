apply plugin: 'java'
apply plugin: 'application'

archivesBaseName = "ready"
version = '1.0'
mainClassName = "Ready"

repositories {
  mavenCentral()
}

dependencies {
  testCompile group: 'junit', name: 'junit', version: '4.+'
}